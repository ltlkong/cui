export type LinkTypes = 'primary' | 'secondary' | 'article' | 'cover'

export interface AvalialeLinkColors {
  main: string
  secondary: string
  tertiary: string
  articleBg: string
  mainGradient: string
}
